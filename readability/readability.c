#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string text = get_string("Text: ");

    int words = 0;
    int letters = 0;
    int sentences = 0;

    int len = strlen(text);

    for(int i=0; i<len; i++){
        if(text[i])
    }

}